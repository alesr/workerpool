name: modernize
on:
  schedule:
    - cron: "0 9 * * 5"
permissions:
  contents: write
  pull-requests: write
jobs:
  modernize:
    runs-on: ubuntu-latest
    steps:
      - uses: alesr/gomodern@v1
        with:
          mode: fix
          create-pr: "true"
